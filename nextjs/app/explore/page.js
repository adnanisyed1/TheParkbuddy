"use client";
import EmbeddedSite from "../components/EmbeddedSite";

// Explore = the interactive Google Map + live weather + "My Trip" cart
// (formerly the homepage). Reached from the landing's Explore / Parks-near-me tiles.
export default function ExplorePage() {
  return <EmbeddedSite page="index" />;
}
